spring:
  datasource:
    url: jdbc:postgresql://localhost:64783/integration-tests-db
    username: sa
    password: sa
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
